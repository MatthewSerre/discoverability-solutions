{
    "name": "react-and-rails",
    "scripts": {
      "build": "cd discoverability-solutions && npm install && npm run build && cd ..",
      "deploy": "cp -a discoverability-solutions/build/. public/",
      "postinstall": "npm run build && npm run deploy && echo 'Client built!'"
    }
  }